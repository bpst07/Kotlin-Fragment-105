package com.arrow.fragment

/**
 * This interface must be implemented by activities that contain this fragment
 * listener to allow an interaction in the fragment to be communicated to the
 * activity and potentially other fragments contained in that activity
 */
interface OnFragmentInteractionListener {
    fun onFragmentInteractionListener(someText: String)
}